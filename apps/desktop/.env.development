# Dev mode: talk to a local dsh-ops process. Use `pnpm dsh --profile ops` from
# the monorepo root with DSH_HOME pointing at a scratch harness home; override
# the URL from the in-app "use a different backend" field if needed.
VITE_DEFAULT_SERVICE_URL=http://127.0.0.1:18000