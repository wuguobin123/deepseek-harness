# Production build: the dsh-ops backend is fronted by nginx on
# http://119.45.252.25:18080/, which proxies /api/* and the SSE carriers to
# the systemd-managed ops profile on 127.0.0.1:18000.
VITE_DEFAULT_SERVICE_URL=http://119.45.252.25:18080