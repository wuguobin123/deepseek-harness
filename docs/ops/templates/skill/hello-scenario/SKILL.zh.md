---
name: hello-scenario
description: 模型在目录中读取的单行摘要；不超过约 120 个字符。
whenToUse: 可选的面向用户提示；模型不可见。
---

# Hello scenario

[English](SKILL.md) | 中文

将此正文替换为父模型加载该 Skill 时应读取的指令。正文使用普通 Markdown；Skill 消费方将其渲染为 `<skill_content>`，并在用户显式操作边界注入。

保持正文简短。Skill 正文位于父级请求上下文中，因此此处的每个 token 都会占用父级上下文窗口。请引用外部文件或工具调用，不要内联大量内容。

## 模型读取此 Skill 后执行什么

1. 判断用户任务是否匹配该场景。
2. 如果匹配，遵循下方指令；否则说明原因并停止。
3. 指令产生工具调用时，由父级批准链控制这些调用。

## 指令

- 将本节替换为模型应遵循的场景专用步骤。
- 优先使用编号步骤，而非普通段落；模型能更可靠地解析列表。
- 场景需要副作用时，明确写出模型应执行的确切工具调用。

## 参考材料

将参考资料、模板或脚本放在此 `SKILL.md` 旁的 `references/` 或 `scripts/` 目录中。Skill 消费方会在正文后渲染资源根目录，并列出可供模型读取的文件。
