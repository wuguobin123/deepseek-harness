/**
 * Local Xiaowei profile bundle marker.
 * @module @deepseek-ai/dsh-xiaowei-local
 */

/** Cordis plugin name. */
export const name = 'xiaowei-local'

/** Apply the marker plugin; behavior is owned by the bundle patch. */
export function apply(): void {}
