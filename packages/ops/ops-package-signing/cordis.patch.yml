# Patch overlay reserved for `@deepseek-ai/dsh-ops-package-signing`. The plugin
# is a no-op skeleton until the first signed bundle is接入ed; uncomment the row
# replace the empty sequence below with the row when the HMAC-SHA256 verifier
# lands.
#
# - id: ops-package-signing
#   name: '@deepseek-ai/dsh-ops-package-signing'

[]
