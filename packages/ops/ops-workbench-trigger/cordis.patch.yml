# Patch overlay reserved for `@deepseek-ai/dsh-ops-workbench-trigger`. The plugin
# is a no-op skeleton until the first scenario that needs cross-session reminders
# is接入ed; replace the empty sequence below with the row when the global trigger
# implementation lands.
#
# - id: ops-workbench-trigger
#   name: '@deepseek-ai/dsh-ops-workbench-trigger'

[]
