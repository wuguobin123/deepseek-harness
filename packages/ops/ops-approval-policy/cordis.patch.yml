# Patch overlay reserved for `@deepseek-ai/dsh-ops-approval-policy`. The plugin
# is a no-op skeleton until the first ops scenario drives the policy resolver;
# replace the empty sequence below with the row when that happens.
#
# - id: ops-approval-policy
#   name: '@deepseek-ai/dsh-ops-approval-policy'

[]
