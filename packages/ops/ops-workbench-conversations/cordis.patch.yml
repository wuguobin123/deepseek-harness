# Patch overlay reserved for `@deepseek-ai/dsh-ops-workbench-conversations`. The plugin
# is a no-op skeleton until the first scenario that needs multi-tenant chat history
# is接入ed; uncomment the row below when the conversations projection lands.
#
# - id: ops-workbench-conversations
#   name: '@deepseek-ai/dsh-ops-workbench-conversations'
