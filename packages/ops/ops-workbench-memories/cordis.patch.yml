# Patch overlay reserved for `@deepseek-ai/dsh-ops-workbench-memories`. The plugin
# is a no-op skeleton until the first scenario that needs cross-session memory
# is接入ed; uncomment the row below when the OpenViking adapter lands.
#
# - id: ops-workbench-memories
#   name: '@deepseek-ai/dsh-ops-workbench-memories'
